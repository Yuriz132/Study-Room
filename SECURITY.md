# 安全政策（Security Policy）

感谢你关注 **Study Room** 的安全。本文档说明本项目的安全模型、部署安全建议与漏洞报告方式。

## 安全架构

- **身份认证**：注册 / 登录使用 Bearer Token；密码经 scrypt 哈希存储；**每次登录轮换 Token**，修改密码后旧 Token 立即失效（强制其他设备下线）。
- **人机验证**：注册与登录接入极验（Geetest 4 代）行为验证，抵御批量注册与撞库。
- **AI 密钥隔离**：所有大模型调用经由后端代理（`/api/ai/*`），API Key 仅存在于**服务端环境变量**，**绝不出现在前端打包产物**中。
- **请求限流**：认证相关接口按 IP 限流（`ipGuard`），缓解暴力尝试。
- **实时通信鉴权**：Socket.IO（在线状态 / 单词 PK / 私信）的连接鉴权复用 Bearer Token。

## 部署安全建议

> ⚠️ 生产环境请务必遵循以下实践，否则存在凭据泄露与账号被盗风险。

1. **强制 HTTPS**：务必配置 TLS（Let's Encrypt 或商业证书）。**禁止在纯 HTTP 下运行**——明文 HTTP 会导致登录 Token、密码哈希在网络中被嗅探拦截。
2. **密钥管理**：
   - `backend/.env`（含 `AGNES_API_KEY` / `MIMO_API_KEY` / `GEETEST_CAPTCHA_KEY` 等）已被 `.gitignore` 忽略，**请勿提交到仓库**。
   - 使用强随机值，并**定期轮换**；不同环境使用不同密钥。
3. **最小权限运行**：后端以非 root 用户（如 `www-data`）运行，并仅授予其对 `backend/data/` 的读写权限（否则登录写库会报 `EACCES`）。
4. **服务器加固**：
   - 使用 **SSH 密钥登录并禁用密码认证**；定期更换服务器 root / 管理员密码。
   - 仅开放必要端口（如 80 / 443），其余通过防火墙收敛。
5. **CORS 收紧**：生产环境将 `CORS_ORIGIN` 设为前端真实域名，**不要长期保留 `*`**。

## 已知注意事项

- 本项目默认使用 **JSON 文件存储**（`backend/data/`，零数据库依赖）。该目录包含用户数据，**已被 gitignore**。请勿将其纳入版本控制或公开分享。
- 上传的媒体文件（`uploads/`）同样不入库。
- 实时功能（PK / 私信 / 在线状态）依赖 WebSocket，请确保反向代理正确配置 `Upgrade` 头。

## 负责任披露

如发现安全漏洞，请**私下**报告（请勿在公开 Issue 中披露细节）：

- **推荐**：在仓库 `Security` 选项卡提交 **Report a vulnerability**（GitHub Security Advisories）。
- 或私信仓库维护者。

我们会尽快确认、修复，并在修复后公开致谢（如你愿意）。

## 免责声明

本项目按「现状」以 MIT 许可提供。部署者需自行负责所在环境的安全与合规（包括但不限于 ICP 备案、数据保护、内容审核）。
