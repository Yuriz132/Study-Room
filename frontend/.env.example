# ============================================================
# Study Room 前端环境变量示例
# 复制本文件为 frontend/.env 并填入真实值
# （.env 已被 .gitignore 忽略，绝不会被提交到仓库）
# 注意：Vite 环境变量必须以 VITE_ 开头，且在「构建时」嵌入产物
# ============================================================

# API 基础地址（后端地址 + /api 前缀）
# 同源部署（如经 nginx 挂在 /vs 子路径）：/vs/api
# 跨域直连后端（开发 / 临时）：http://your-server-ip:3000/api
VITE_API_BASE_URL=/vs/api

# 极验前端 ID（公开，从极验后台获取；与后端 GEETEST_CAPTCHA_KEY 成对）
VITE_GEETEST_CAPTCHA_ID=
